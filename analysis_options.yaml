include: package:pedantic/analysis_options.yaml

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    invalid_annotation_target: ignore

linter:
  rules:
    - cancel_subscriptions # Confirm StreamSubscription cancel
    - close_sinks # Confirm Sink close